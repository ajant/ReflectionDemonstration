<!DOCTYPE phpunit>
<phpunit colors="true">
    <testsuite name="All Tests">
        <directory>.</directory>
    </testsuite>
    <filter>
        <whitelist>
            <directory suffix=".php">./</directory>
        </whitelist>
    </filter>
</phpunit>